{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-center">Bienvenue sur EventHub 🎉</h1>

        {% if app.user %}
            <p class="text-center">Bonjour {{ app.user.prenom }} {{ app.user.nom }} 👋</p>
            <div class="text-center">
                <a href="{{ path('app_profile_edit', {'id': app.user.id}) }}" class="btn btn-primary">Mon Profil</a>
            </div>
        {% else %}
            <p class="text-center">Connectez-vous ou inscrivez-vous pour découvrir et créer des événements !</p>
            <div class="text-center">
                <a href="{{ path('app_login') }}" class="btn btn-success me-2">Connexion</a>
                <a href="{{ path('app_register') }}" class="btn btn-outline-primary">Inscription</a>
            </div>
        {% endif %}
    </div>
{% endblock %}
